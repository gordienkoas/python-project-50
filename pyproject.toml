[project]
name = "hexlet-code"
version = "0.1.0"
description = "Gendiff. My second project."
authors = ["Gordienko Andrey"]
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
packages = [
    { include = "gendiff"}
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["gendiff"]

[project.scripts]
gendiff = "gendiff.scripts.gendiff:main"